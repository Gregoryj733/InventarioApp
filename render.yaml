# Render Blueprint — plan gratuito
# Dashboard: https://dashboard.render.com/blueprints

services:
  - type: web
    name: inventario-sync-totalcare
    runtime: docker
    rootDir: sync-server
    plan: free
    region: ohio
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: API_KEY
        value: TcSync-7kM9pQ2xR4nW
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: FIREBASE_SERVICE_ACCOUNT
        sync: false
